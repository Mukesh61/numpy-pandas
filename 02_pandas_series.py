import numpy as np
import pandas as pd

data = pd.Series(np.random.randint(10,10,2000))
'''
what is vectorization? it is also know as array programming. instead of process data one by one in loop, vectorization perform at once. like in pandas and numpy.
'''

